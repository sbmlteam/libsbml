/**
 * @file:   ArraysEventAssignmentPlugin.cpp
 * @brief:  Implementation of the ArraysEventAssignmentPlugin class
 * @author: Generated by autocreate code
 *
 * <!--------------------------------------------------------------------------
 * This file is part of libSBML.  Please visit http://sbml.org for more
 * information about SBML, and the latest version of libSBML.
 *
 * Copyright (C) 2009-2013 jointly by the following organizations:
 *     1. California Institute of Technology, Pasadena, CA, USA
 *     2. EMBL European Bioinformatics Institute (EBML-EBI), Hinxton, UK
 *
 * Copyright (C) 2006-2008 by the California Institute of Technology,
 *     Pasadena, CA, USA 
 *
 * Copyright (C) 2002-2005 jointly by the following organizations:
 *     1. California Institute of Technology, Pasadena, CA, USA
 *     2. Japan Science and Technology Agency, Japan
 *
 * This library is free software; you can redistribute it and/or modify it
 * under the terms of the GNU Lesser General Public License as published by
 * the Free Software Foundation.  A copy of the license agreement is provided
 * in the file named "LICENSE.txt" included with this software distribution
 * and also available online as http://sbml.org/software/libsbml/license.html
 * ------------------------------------------------------------------------ -->
 */


#include <sbml/packages/arrays/extension/ArraysEventAssignmentPlugin.h>


using namespace std;


#ifdef __cplusplus


LIBSBML_CPP_NAMESPACE_BEGIN


/*
 * Creates a new ArraysEventAssignmentPlugin
 */
ArraysEventAssignmentPlugin::ArraysEventAssignmentPlugin(const std::string& uri,  
                                 const std::string& prefix, 
                               ArraysPkgNamespaces* arraysns) :
	  SBasePlugin(uri, prefix, arraysns)
	, mIndexs (arraysns)
{
}


/*
 * Copy constructor for ArraysEventAssignmentPlugin.
 */
ArraysEventAssignmentPlugin::ArraysEventAssignmentPlugin(const ArraysEventAssignmentPlugin& orig) :
	  SBasePlugin(orig)
	, mIndexs ( orig.mIndexs)
{
}


/*
 * Assignment operator for ArraysEventAssignmentPlugin.
 */
ArraysEventAssignmentPlugin& 
ArraysEventAssignmentPlugin::operator=(const ArraysEventAssignmentPlugin& rhs)
{
	if (&rhs != this)
	{
		this->SBasePlugin::operator=(rhs);
		mIndexs = rhs.mIndexs;
	}

	return *this;
}


/*
 * Creates and returns a deep copy of this ArraysEventAssignmentPlugin object.
 */
ArraysEventAssignmentPlugin* 
ArraysEventAssignmentPlugin::clone () const
{
	return new ArraysEventAssignmentPlugin(*this);
}


/*
 * Destructor for ArraysEventAssignmentPlugin.
 */
ArraysEventAssignmentPlugin::~ArraysEventAssignmentPlugin()
{
}


//---------------------------------------------------------------
//
// overridden virtual functions for read/write/check
//
//---------------------------------------------------------------

/*
 * create object
 */
SBase*
ArraysEventAssignmentPlugin::createObject (XMLInputStream& stream)
{
	SBase* object = NULL; 

	const std::string&      name   = stream.peek().getName(); 
	const XMLNamespaces&    xmlns  = stream.peek().getNamespaces(); 
	const std::string&      prefix = stream.peek().getPrefix(); 

	const std::string& targetPrefix = (xmlns.hasURI(mURI)) ? xmlns.getPrefix(mURI) : mPrefix;

	if (prefix == targetPrefix) 
	{ 
		if (name == "orderedListOfIndices" ) 
		{ 
			object = &mIndexs;

			if (targetPrefix.empty() == true) 
			{ 
				mIndexs.getSBMLDocument()->enableDefaultNS(mURI, true); 
			} 
		} 
	} 

	return object; 
}


/*
 * write elements
 */
void
ArraysEventAssignmentPlugin::writeElements (XMLOutputStream& stream) const
{
	if (getNumIndexs() > 0) 
	{ 
		mIndexs.write(stream);
	} 
}


/*
 * Checks if this plugin object has all the required elements.
 */
bool
ArraysEventAssignmentPlugin::hasRequiredElements () const
{
	bool allPresent = true; 

	// TO DO 

	return allPresent; 
}


//---------------------------------------------------------------
//
// Functions for interacting with the members of the plugin
//
//---------------------------------------------------------------

/*
 * Returns the ListOfIndices in this plugin object.
 */
const ListOfIndices* 
ArraysEventAssignmentPlugin::getListOfIndices () const
{
	return static_cast<const ListOfIndices*>(&this->mIndexs);
}


/*
 * Returns the ListOfIndices in this plugin object.
 */
ListOfIndices* 
ArraysEventAssignmentPlugin::getListOfIndices ()
{
	return &this->mIndexs;
}


/*
 * Returns the Index object that belongs to the given index.
 */
const Index*
ArraysEventAssignmentPlugin::getIndex(unsigned int n) const
{
	return static_cast<const Index*>(mIndexs.get(n));
}


/*
 * Returns the Index object that belongs to the given index.
 */
Index*
ArraysEventAssignmentPlugin::getIndex(unsigned int n)
{
	return static_cast<Index*>(mIndexs.get(n));
}


/*
 * Returns the Index object based on its identifier.
 */
const Index*
ArraysEventAssignmentPlugin::getIndex(const std::string& sid) const
{
	return static_cast<const Index*>(mIndexs.get(sid));
}


/*
 * Returns the Index object based on its identifier.
Index*
ArraysEventAssignmentPlugin::getIndex(const std::string& sid)
{
	return static_cast<const Index*>(mIndexs.get(sid));
}


/*
 * Adds a copy of the given Index to the ListOfIndices in this plugin object.
 */
int
ArraysEventAssignmentPlugin::addIndex (const Index* index)
{
	if (index == NULL)
	{
		return LIBSBML_OPERATION_FAILED;
	}
	else if (index->hasRequiredElements() == false)
	{
		return LIBSBML_INVALID_OBJECT;
	}
	else if (getLevel() != index->getLevel())
	{
		return LIBSBML_LEVEL_MISMATCH;
	}
	else if (getVersion() != index->getVersion())
	{
		return LIBSBML_VERSION_MISMATCH;
	}
	else if (getPackageVersion() != index->getPackageVersion())
	{
		return LIBSBML_PKG_VERSION_MISMATCH;
	}
	else
	{
		mIndexs.append(index);
	}

	return LIBSBML_OPERATION_SUCCESS;

}


/*
 * Creates a new Index object and adds it to the ListOfIndices in this plugin object.
 */
Index* 
ArraysEventAssignmentPlugin::createIndex ()
{
	 Index* i = NULL;

	try
	{
		ARRAYS_CREATE_NS(arraysns, getSBMLNamespaces());
		i = new Index(arraysns);
	}
	catch(...)
	{
	}

	if (i != NULL)
	{
		mIndexs.appendAndOwn(i);
	}

	return i;
}


/*
 * Removes the nth Index object from this plugin object
 */
Index* 
ArraysEventAssignmentPlugin::removeIndex(unsigned int n)
{
	return static_cast<Index*>(mIndexs.remove(n));
}


/*
 * Removes the Index object with the given id from this plugin object
 */
Index* 
ArraysEventAssignmentPlugin::removeIndex(const std::string& sid)
{
	return static_cast<Index*>(mIndexs.remove(sid));
}


/*
 * Returns the number of Index objects in this plugin object.
 */
unsigned int 
ArraysEventAssignmentPlugin::getNumIndexs () const
{
	return mIndexs.size();
}


//---------------------------------------------------------------


/*
 * Set the SBMLDocument.
 */
void
ArraysEventAssignmentPlugin::setSBMLDocument(SBMLDocument* d)
{
	SBasePlugin::setSBMLDocument(d);

	mIndexs.setSBMLDocument(d);
}


/*
 * Connect to parent.
 */
void
ArraysEventAssignmentPlugin::connectToParent(SBase* sbase)
{
	SBasePlugin::connectToParent(sbase);

	mIndexs.connectToParent(sbase);
}


/*
 * Enables the given package.
 */
void
ArraysEventAssignmentPlugin::enablePackageInternal(const std::string& pkgURI,
                                   const std::string& pkgPrefix, bool flag)
{
	mIndexs.enablePackageInternal(pkgURI, pkgPrefix, flag);
}




LIBSBML_CPP_NAMESPACE_END


#endif /* __cplusplus */


