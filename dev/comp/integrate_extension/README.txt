This CMake file can be used to create the comp extension as a separate library 
that links to the core libsbml library.

It is a bit specific to the user so I have moved it out of the main src tree. 